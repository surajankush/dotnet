version: 0.2

phases:
    install:
        runtime-versions:
            dotnet: 3.1
    build:
        commands:
            - dotnet --info
            - dotnet build 
            - zip -r filename.zip /codebuild/output/src279334977/src/bin/Debug/netcoreapp3.1
artifacts:
    files:
        - filename.zip