version: 0.2

phases:
    install:
        runtime-versions:
            dotnet: 3.1
    build:
        commands:
            - dotnet --info
            - dotnet build 
            - dotnet publish
            - ls
artifacts:
    files:
        - bin\Debug
